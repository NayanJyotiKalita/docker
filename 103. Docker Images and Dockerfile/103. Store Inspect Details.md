# Store Inspect Details

**Objective: Deploy an Nginx container using Docker and extract detailed configuration information through a series of inspection tasks. This will enhance your understanding of Docker container configurations.

Tasks:

Deploy Container: Run Nginx in detached mode, create a container named my_nginx with latest nginx image.
Save Configuration: Export the full container configuration to a JSON file /home/user/answer.json.
Extract IP Address: Retrieve the container’s IP address with inspect command.
Investigate Mount Points: Output mount points in JSON format and format them with jq.
